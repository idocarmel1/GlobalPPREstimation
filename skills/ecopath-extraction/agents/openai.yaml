interface:
  display_name: "Ecopath Extraction"
  short_description: "Extract and validate published EwE models"
  default_prompt: "Use $ecopath-extraction to extract this Ecopath model with page-level provenance and validation."
