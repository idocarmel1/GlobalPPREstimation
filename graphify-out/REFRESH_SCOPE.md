This refresh indexes the project's architecture and reviewed scientific evidence. The explicit `.graphifyignore` keeps the corpus below 500 files and two million words. The portable manifest records the complete selected corpus using repository-relative paths and content hashes.

Included: the three project source trees and their tests; integration tools and tests; current project documentation; reviewed mapping notes, model profiles and work orders; the isolated new-paper validation's original reproduction code and reports; the durable independent Guinea comparison reports; canonical Claude extraction and mapping resources; the combined router and taxonomy source; both Claude and Codex skill entry points; and Codex workflow/interface wrappers. Copied implementation resources are represented by their canonical source and the distribution documentation.

Excluded: generated spreadsheets, CSV/JSON numeric data, images, PDFs, raw paper archives, downloaded research, generated atlas HTML, notebook exports, packages, caches, scratch work, personal settings, obsolete model catalogs and duplicate evaluation/tool/skill copies. These artifacts remain in the project; the graph links their governing code and reviewed conclusions rather than converting thousands of regenerated cells or pages into duplicate text.

Structural extraction parses code without executing any project algorithm. Changed documents are read by host semantic-extraction agents using the graphify confidence rubric. Unchanged graph entities and their relationships are retained. Old source paths are normalized; deleted, excluded and changed sources are pruned from the old graph before fresh extraction is merged. Existing hyperedges and original directional edge endpoints are preserved. Fuzzy entity deduplication is disabled to avoid merging similarly named symbols from different files.

The installed graphify version prunes after merging when `build_merge(prune_sources=...)` is used, which would also discard newly extracted nodes from changed files. The refresh helper therefore filters only the old graph first and then uses `build_merge` on that retained graph. This is a graph-maintenance workaround; no PPR algorithm or Jensen calculation is changed.

Semantic token usage and monetary cost are unavailable because the host subagent tool exposes no usage totals. Placeholder zeros required by the extraction schema are not reported as measured usage or zero cost. Historical cost records are preserved but are not independently verified.

Graph validation checks endpoint integrity, portable and existing source paths, removal of personal settings, all six skill entry points, the final integration and independent-comparison reports, and the requested PPR-scope, model-health, provenance and Jensen concepts. Presence in the graph is a retrieval check; it is not proof of scientific validity or numerical correctness.

The prior integration refresh, completed before the annual graph feature, contained 2,082 nodes, 3,256 edges, 13 hyperedges and 143 named communities across 229 files. Its browser check and 68.4x heuristic benchmark describe that earlier snapshot.

The current annual-view increment re-extracted six code files with AST parsing (60 nodes, 122 edges) and four changed documents through one host semantic agent (120 nodes, 292 edges, three hyperedges). Unchanged semantic evidence was retained. The complete corpus is now 235 files and 289,433 words. The final graph contains 2,240 nodes, 3,610 edges, 16 hyperedges and 153 named communities; every selected source is represented.

This increment preserves 2,061 unchanged nodes and 3,220 unchanged-source edges, including 8 links to refreshed nodes with restored IDs. Six old relationships lose a genuinely absent endpoint in the final extraction and are pruned; no unchanged-source edge pair has conflicting new provenance. Earlier HANDOFF status is explicitly historical; sections 14 and 15 record the integration and annual-view completion.

The annual concepts distinguish catch-taxon TL from model TL, separate model versions, source scopes and failed or missing estimates. Regional curves use a complete fixed ecosystem cohort over the chosen year range. Single-ecosystem curves retain their available span and internal gaps. PPR/NPP uses 100 × sum(PPR wet weight) / 9 / sum(NPP carbon) for the same eligible ecosystems; the 2019 NPP baseline is repeated across years by the owner's explicit request. Regional ensemble medians are summed. Geographic overlap, exclusions and coverage remain disclosed. This graph update does not execute project calculations, change algorithms or alter Jensen behavior.

Existing eight AMBIGUOUS relationships remain visible, including the three source-attributed USER_GUIDE versus SPPR_Methods conflicts about fractional trophic-level bias, Ulanowicz equivalence and net migration sign. They remain review questions, not simultaneous confirmed scientific claims.

The current benchmark uses the detected 289,433-word corpus and eight project-specific queries. Its estimated average query context is 6,902 tokens versus about 385,910 for the full corpus (55.9x reduction). These are heuristic context-size estimates, not measured agent usage, answer accuracy or monetary savings. Actual semantic usage and monetary cost remain unavailable.


Final graph verification independently checked all 3,220 retained edge attributes and original directions, and all 13 existing hyperedges; the three new hyperedges bring the total to 16. Source SHA-256 hashes match all 235 files. All ten changed sources and all twelve targeted annual-feature concepts are represented, and a trend query retrieved 73 related nodes. The confidence totals are 3,496 EXTRACTED, 106 INFERRED and eight AMBIGUOUS edges. The parent integration agent browser-verified the graph counts and NPP search results, including the fixed 2019 baseline, matched-cohort percentage, regional medians and future annual arrays.

The final incremental detector reports zero new/changed files and zero deleted files across the 235-file corpus.
