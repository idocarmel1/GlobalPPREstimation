# core numerics
numpy>=1.24
pandas>=2.0
scipy>=1.10

# raster / vector
xarray>=2023.1
netCDF4>=1.6            # reads the Copernicus NetCDF4 files
pyhdf>=0.11             # reads the OSU HDF4 files; needs libhdf4 on some platforms
geopandas>=0.13
shapely>=2.0            # 2.x for make_valid
exactextract>=0.2       # exact cell/polygon coverage fractions
pyproj>=3.4             # geodesic areas, used by the validation checks

# outputs / config
openpyxl>=3.1
PyYAML>=6.0
certifi>=2024.2.2

# tests
pytest>=7.0
